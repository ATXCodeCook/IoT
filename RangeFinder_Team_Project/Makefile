# Makefile for example Arduino projects

build:
	cd cRangeFinder && make
	cd aRangeFinder && make

clean:
	cd cRangeFinder && make clean
	cd aRangeFinder && make clean

install:
	sudo cp include/AVR* /usr/local/include
